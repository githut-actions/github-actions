name: checkout actions
on: [push]
jobs:
    checkout:
      runs-on: ubuntu-latest
      steps:
        - name: checkout
          uses: actions/checkout@v5
        - name: display the files
          run: ls -la
