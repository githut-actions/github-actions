name: concurrency
on: [push]
concurrency:
  # this workflow should be triggred for any branch only onces
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true # exsting will cancle and latest will run
jobs:
  concurrency:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: sleep 20 